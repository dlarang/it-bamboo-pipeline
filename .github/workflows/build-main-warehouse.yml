name: Build Main Warehouse

on:
  push:
    branches:
      - main
  workflow_dispatch: #Allows manual trigger

jobs:
  Build-Main-Warehouse:
    runs-on: windows-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2    
    - name: Build
      uses: ./.github/templates/build/warehouse
      with:
        branchName: master
        cleanCheckout: true