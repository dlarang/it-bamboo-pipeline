name: Build CI Main CWS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    uses: dlarang/it-bamboo-pipeline/.github/workflows/template-build-cws.yml@main
    with:
      branchName: master
      dbTarget: rc
      msBuildConfig: Release
      targetOption: 'Compile,PrecompileWeb,RunTests'