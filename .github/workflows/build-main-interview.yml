name: Build Main Interview

on:
  push:
    branches:
      - main
  workflow_dispatch: #Allows manual trigger

jobs:
  Build-Main-Interview:
    runs-on: windows-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2    
    - name: Build
      uses: ./.github/templates/build/interview
      with:
        branchName: master
        cleanCheckout: true